# =============================================================================
# Forgejo Handlers
# =============================================================================

---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart forgejo
  ansible.builtin.systemd:
    name: "{{ forgejo_service_name }}"
    state: restarted
